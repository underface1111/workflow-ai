PORT=3000
BASE_URL=http://localhost:3000

# SonarCloud — local scanner only; CI uses GitHub Secrets (not this file)
# SONAR_HOST_URL=https://sonarcloud.io
# SONAR_TOKEN=
# SONAR_ORGANIZATION=underface1111

# GitHub repo → Settings → Secrets and variables → Actions:
#   SONAR_TOKEN, SONAR_HOST_URL=https://sonarcloud.io

# AI Test Agent (Bước B)
# Tạm thời: npm run ai:test-gen (template) + npm run ai:test-validate
# Khi có credit: bỏ comment key → npm run ai:test-gen:llm

# ANTHROPIC_API_KEY=
# ANTHROPIC_MODEL=claude-haiku-4-5

# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
